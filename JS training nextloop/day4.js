// JS Functions. 

// Functions--
// -A JavaScript function is a block of code designed to perform a particular task.
// -They are executed when something invokes it or calls it.
// -A JS function is defined with the function keyword, followed by a function name & parentheses().
// -The parentheses() may include a params name such as (para1, para2, so on).
// -The code to be executed by a function is placed inside a curly braces{}.

// Syntax-
// function funName(parameter1, parameter2,...){
//     //code;
// }
// funName(arguments1,arguments2);

// -We can call the functions with funName then parentheses(). 
// -Parameters are listed inside the () in the function definition.
// -Arguments are the values received by the function when it is invoked.

// function test(){
//     console.log("Hellooo");
// }
// test();

// Function Return- 
// -When JS reaches a return statement, the function will stop executing.
// function sum(a,b){
//     return a+b;
// }
// console.log(sum(2,5));